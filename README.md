# FlowZapp
AI SOP & Knowledge Base Builder — screen recording → AI-drafted SOP → approved, searchable, answerable.

- `Documentation/` — the specification set (start with 12-Project-Overview)
- `api/` — Laravel 12 API (see `api/SETUP.md`)
- `web/` — Angular 21 + PrimeNG
- `spike/generation-eval/` — transcription/generation evaluation, run before M1
- `CLAUDE.md` — the standing contract for AI-assisted development
