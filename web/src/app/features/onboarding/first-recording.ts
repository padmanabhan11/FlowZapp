import { Component } from '@angular/core';

/** S4 placeholder — built in M1 with the capture modal (S11). */
@Component({
  selector: 'app-first-recording',
  template: `
    <section class="document-surface" style="max-width: 560px; margin: 64px auto; padding: 32px; text-align: center">
      <h1>What do you explain most often?</h1>
      <p style="color: var(--ink-soft)">Recording arrives in M1. The workspace is ready.</p>
    </section>
  `,
})
export class FirstRecording {}
